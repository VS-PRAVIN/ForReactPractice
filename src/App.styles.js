import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    // alignItems: 'center',
    // justifyContent: 'center',
    paddingTop: 50,
  },
  statusText: {
  textAlign: 'center',
  fontSize: 16,
  fontWeight: 'bold',
  color: '#555',
  marginVertical: 10,
}
});